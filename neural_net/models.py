from django.db import models

# Create your models here.

#the model (queries) is going to store information about previous queries.
#schema:
    #query
    #result
    #user_id

#If theorizer is going to add each query to the training set, that will be another
#thing to do here.

#Admin can schedule the training to be redone every week or something like that.
